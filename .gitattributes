git config --global core.autocrlf false
```

**2. If using VS Code, set default to LF:**
- Go to **File > Preferences > Settings**
- Search for `eol`
- Set `Files: Eol` to **`\n`**

**3. Add a `.gitattributes` file to your repo** (most reliable fix):
```
* text=auto eol=lf